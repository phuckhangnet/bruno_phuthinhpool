info:
  name: Cập kho số lượng
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/Product/UpdateAmount"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: |-
      {
          "ProductId":31,
          "Amount": 200
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
