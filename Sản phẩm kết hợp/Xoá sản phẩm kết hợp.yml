info:
  name: Xoá sản phẩm kết hợp
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/ProductCombos/Delete"
  headers:
    - name: x-api-key
      value: pkn
  body:
    type: json
    data: |-
      {
         "comboId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
