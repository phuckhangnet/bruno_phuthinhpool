info:
  name: Lấy sản phẩm theo khách hàng đang thuê
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/Customer/Products/GetAll/Renting"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: |-
      {
          "CustomerID": 102,
          "SearchText": "",
          "PageNumber": 1,
          "PageSize": 10,
          "Sort": "desc" // ASC | DESC || null - Sắp xếp theo tên sp.
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
