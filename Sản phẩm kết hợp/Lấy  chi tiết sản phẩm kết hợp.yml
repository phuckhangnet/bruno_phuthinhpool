info:
  name: Lấy  chi tiết sản phẩm kết hợp
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/ProductCombos/Detail"
  headers:
    - name: x-api-key
      value: pkn
  body:
    type: json
    data: |-
      {
         "ComboId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
